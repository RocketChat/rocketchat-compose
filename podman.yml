services:
  opentelemetry-logs-collector:
    security_opt:
      - label=disable
    volumes:
      - ${XDG_RUNTIME_DIR}/podman/podman.sock:/var/run/docker.sock:ro
      - ${HOME}/.local/share/containers/storage/overlay-containers:/hostfs/storage:ro
